# =============================================================================
# ThreadForge - Configuration Production
# =============================================================================
# Copiez ce fichier vers .env et remplissez les valeurs
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# BASE DE DONNÉES POSTGRESQL
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGEZ_MOI_mot_de_passe_securise_32_caracteres
POSTGRES_DB=threadforge

# -----------------------------------------------------------------------------
# AUTHENTIFICATION JWT
# -----------------------------------------------------------------------------
# Secret pour signer les tokens JWT (minimum 32 caractères)
# Générer avec: openssl rand -base64 32
JWT_SECRET=CHANGEZ_MOI_secret_jwt_minimum_32_caracteres_aleatoires

# Émetteur et audience du token (utilisez votre domaine quand disponible)
JWT_ISSUER=https://localhost
JWT_AUDIENCE=https://localhost

# -----------------------------------------------------------------------------
# PROTECTION GATEWAY (Mot de passe pour accéder à l'application)
# -----------------------------------------------------------------------------
# Ce mot de passe protège l'accès à toute l'application
# Les utilisateurs devront le saisir pour accéder au site
GATEWAY_PASSWORD=CHANGEZ_MOI_mot_de_passe_gateway

# -----------------------------------------------------------------------------
# API XAI (GROK)
# -----------------------------------------------------------------------------
# Clé API xAI pour la génération de threads
# Obtenez votre clé sur: https://console.x.ai/
XAI_API_KEY=xai-VOTRE_CLE_API_ICI

# Modèle à utiliser (grok-3-mini recommandé pour le MVP - moins cher)
# Options: grok-3-mini, grok-3, grok-2-latest
XAI_MODEL=grok-3-mini

# -----------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# -----------------------------------------------------------------------------
# Origine autorisée pour les requêtes API
# Pour un accès via IP: http://VOTRE_IP_VPS
# Pour un domaine: https://votre-domaine.com
CORS_ORIGIN=http://localhost

# -----------------------------------------------------------------------------
# CONFIGURATION SERVEUR
# -----------------------------------------------------------------------------
# Hôtes autorisés (* = tous, ou votre domaine)
ALLOWED_HOSTS=*

# -----------------------------------------------------------------------------
# DOMAINE (Pour HTTPS - optionnel pour le moment)
# -----------------------------------------------------------------------------
# Décommentez et configurez quand vous avez un domaine
# DOMAIN=votre-domaine.com
# EMAIL=votre-email@exemple.com
